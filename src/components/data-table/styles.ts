import { styled } from "@mui/material/styles";
import { DataGrid } from "@mui/x-data-grid";

export const StyledDataGrid = styled(DataGrid)(() => ({
  "& .task--to-do": {}, // TO DO
  "& .task--completed": {}, // TO DO
  "& .task--in-progress": {}, // TO DO
}));
